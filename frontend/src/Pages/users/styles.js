import styled from "styled-components";
import tw from "twin.macro";

export const LoginSignUp = styled.section`
${tw`min-h-screen py-20 flex items-center justify-center bg-[rgba(140, 104, 204, .3)]`}
`