import React from 'react'
import { EventWrapper } from '../../all/frames/EventWrapper'
import Event from "./Event"
const Wrapper = () => {
  return (
    <EventWrapper element={<Event/>}/>
  )
}

export default Wrapper